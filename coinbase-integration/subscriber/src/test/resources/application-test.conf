include "application.conf"

application {
    host = "localhost"
    port = 8081
}

rethinkdb {
    port = ${?RETHINKDB_PORT}
    host = "localhost"
}

coinbase {
    wsFeedUri = ${?COINBASE_WS_FEED_URI}
}
