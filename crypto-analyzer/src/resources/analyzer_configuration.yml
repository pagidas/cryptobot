coinbase_adapter:
    subscribe: /api/coinbase/wsFeed/subscribe
    auth: /api/auth
    subscriptions: /api/subscriptions
    host: localhost
    port: 9000
    subscribe_body:
        type: Subscribe
        productIds: []
        channels:
            - ticker

rethinkdb:
    host: localhost
    port: 28015
    db_name: cryptobot

graphql:
    host: localhost
    port: 9090
    endpoints:
        base: /graphql

coinbase_subscriber:
    host: localhost
    port: 9999
    endpoints:
        subscribe: /subscribe
