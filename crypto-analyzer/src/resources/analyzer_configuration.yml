coinbase_adapter:
    subscribe_products_endpoint: /api/market/subscribe
    host: localhost
    port: 9000
    post_request_body_template:
        type: Subscribe
        productIds: []
        channels:
            - ticker

rethinkdb:
    host: localhost
    port: 28015
    db_name: cryptobot
